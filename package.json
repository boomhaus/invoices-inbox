{
  "name": "invoices-inbox",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Unattended invoice intake: Gmail -> classification/extraction -> Accounting shared drive",
  "scripts": {
    "build": "tsc --noEmit && node tools/build.mjs",
    "test": "vitest run",
    "lint": "eslint src",
    "push": "npm run build && npm run lint && clasp push"
  },
  "devDependencies": {
    "@google/clasp": "^3.3.0",
    "@types/google-apps-script": "^2.0.11",
    "@typescript-eslint/parser": "^8.65.0",
    "esbuild": "^0.28.1",
    "eslint": "^9.39.5",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
